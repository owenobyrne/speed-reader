---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Electron project with working window and build pipeline.

Purpose: Establish the application foundation that all subsequent phases build upon.
Output: Running Electron app with basic window displaying black background.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Constraints from PROJECT.md:**
- Cross-platform: Electron targeting Mac, Windows, Linux
- Offline-capable
- Performance-critical timing (foundation should be lean)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Electron project</name>
  <files>package.json</files>
  <action>
    Run `npm init -y` to create package.json.
    Install electron as dev dependency: `npm install --save-dev electron`.
    Update package.json:
    - Set "main": "main.js"
    - Add scripts: "start": "electron .", "build": "electron-builder"
    - Set appropriate name, version, description
    Do NOT install electron-builder yet (not needed for dev).
  </action>
  <verify>npm run start fails gracefully (main.js doesn't exist yet) or package.json has correct structure</verify>
  <done>package.json exists with electron dependency and start script</done>
</task>

<task type="auto">
  <name>Task 2: Create main process with BrowserWindow</name>
  <files>main.js</files>
  <action>
    Create main.js with:
    - Import app, BrowserWindow from electron
    - createWindow function that creates BrowserWindow (800x600 default size)
    - Set webPreferences: nodeIntegration false, contextIsolation true (security defaults)
    - Load index.html from same directory
    - Handle app 'ready' event to create window
    - Handle 'window-all-closed' to quit (except macOS convention: keep dock icon)
    - Handle 'activate' for macOS dock click to recreate window
    Keep it minimal - no IPC, no preload script yet.
  </action>
  <verify>File exists with correct structure</verify>
  <done>main.js creates BrowserWindow and handles app lifecycle events</done>
</task>

<task type="auto">
  <name>Task 3: Create basic renderer with black background</name>
  <files>index.html</files>
  <action>
    Create index.html with:
    - DOCTYPE html, basic structure
    - Title: "Speed Reader"
    - Inline style or style tag: body with black background (#000), no margin
    - Placeholder centered text in white/gray: "Speed Reader" (will be replaced in Phase 2)
    - meta charset UTF-8
    Keep minimal - no external CSS files yet, this is just foundation.
  </action>
  <verify>`npm run start` launches Electron window with black background</verify>
  <done>Electron app launches, displays black window with placeholder text</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run start` launches Electron window
- [ ] Window displays black background
- [ ] Window can be closed normally
- [ ] App quits when window closed (except macOS keeps dock icon)
</verification>

<success_criteria>

- All three tasks completed
- Electron app launches without errors
- Black window displays as foundation for Phase 2 RSVP display
- No console errors on launch
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
